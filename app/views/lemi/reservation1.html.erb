<!DOCTYPE html>
<html>
<head>
	<meta charset="utf-8">
	<title>contact</title>
	<%= stylesheet_link_tag "reservation1" %>
</head>

<body>

	<%= render :partial => "header" %>

	<div id="backsheet">
		<%= render "errors", obj: @user %>
		<h1>事前予約を始めます</h1>
		<div class="DAYS">
			<p>参加希望日</p>
		</div>
		<%= form_for @user, :url => {:action => 'reservation2'} do |f| %>
		<div>
			<%= select :info, :date, {"お選びください" => "","10月28日(土)" => 1,"10月29日(日)" => 2},{},{class: 'days'} %>
		</div>
		<div class="PEOPLE">
			<p>参加人数</p>
		</div>
		<div>
			<%= f.select :num, {"お選びください" => "","1人" => 1, "2人" => 2, "3人" => 3},{},{class: 'people'} %>
		</div>
		<div class="BUTTEN">
			<%= f.submit '次に進む', class: "butten" %>		
		</div>
		<% end %>
	</div>
</body>

</html>
